import React from 'react';


const Loading = (props) => (
    <div className="container main loading-holder">
        <div className="row">
            < img src="loading.gif" alt="loading" className="loading"/>
        </div>
    </div>
);

export default Loading;
