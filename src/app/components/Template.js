import React from 'react';

const Template = (props) => (
    <div className="container">
        <div className="row">
            <h2 onClick={props.changePage}>Template</h2>
        </div>
    </div>

);

export default Template;
